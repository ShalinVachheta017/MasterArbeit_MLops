name: CD

on:
  push:
    tags:
      - "v*.*.*"
  workflow_dispatch:
    inputs:
      image_tag:
        description: "Optional image tag to deploy/smoke-test (defaults to current ref name)"
        required: false
        type: string

permissions:
  contents: read
  packages: write

concurrency:
  group: cd-${{ github.ref }}
  cancel-in-progress: false

env:
  REGISTRY: ghcr.io
  IMAGE_NAME: shalinvachheta017/masterarbeit_mlops/har-inference

jobs:
  build-and-push:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    outputs:
      image_tag: ${{ steps.vars.outputs.image_tag }}

    steps:
      - uses: actions/checkout@v4

      - name: Resolve image tag
        id: vars
        run: |
          if [ "${{ github.event_name }}" = "workflow_dispatch" ] && [ -n "${{ inputs.image_tag }}" ]; then
            echo "image_tag=${{ inputs.image_tag }}" >> "$GITHUB_OUTPUT"
          else
            echo "image_tag=${{ github.ref_name }}" >> "$GITHUB_OUTPUT"
          fi

      - uses: docker/setup-buildx-action@v3

      - uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build and push image
        uses: docker/build-push-action@v6
        with:
          context: .
          file: docker/Dockerfile.inference
          push: true
          tags: ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:${{ steps.vars.outputs.image_tag }}

  smoke-test:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    needs: build-and-push

    steps:
      - name: Pull image by tag
        run: docker pull ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:${{ needs.build-and-push.outputs.image_tag }}

      - name: Start container
        run: |
          docker run -d --name har-test -p 8000:8000 \
            ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:${{ needs.build-and-push.outputs.image_tag }}

      - name: Debug endpoints
        run: |
          echo "Waiting 30s for TensorFlow + model load..."
          sleep 30
          echo "---- GET / ----"
          curl -i http://localhost:8000/ || true
          echo ""
          echo "---- GET /health ----"
          curl -i http://localhost:8000/health || true
          echo ""
          echo "---- GET /api/health ----"
          curl -i http://localhost:8000/api/health || true
          echo ""
          echo "---- GET /docs ----"
          curl -i http://localhost:8000/docs || true
          echo ""
          echo "---- GET /openapi.json ----"
          curl -i http://localhost:8000/openapi.json || true
          echo ""
          echo "---- container logs ----"
          docker logs har-test || true

      - name: Verify health endpoint
        run: |
          echo "Waiting for API to start..."
          for i in $(seq 1 60); do
            if curl -fsS http://localhost:8000/health > /dev/null; then
              echo "Health OK (/health)"
              exit 0
            fi
            if curl -fsS http://localhost:8000/api/health > /dev/null; then
              echo "Health OK (/api/health)"
              exit 0
            fi
            sleep 2
          done

          echo "Health check failed after 120s. Final debug:"
          echo "---- GET / ----"
          curl -i http://localhost:8000/ || true
          echo "---- GET /health ----"
          curl -i http://localhost:8000/health || true
          echo "---- GET /api/health ----"
          curl -i http://localhost:8000/api/health || true
          echo "---- GET /docs ----"
          curl -i http://localhost:8000/docs || true
          echo "---- GET /openapi.json ----"
          curl -i http://localhost:8000/openapi.json || true
          echo "---- container logs (last 100 lines) ----"
          docker logs --tail 100 har-test || true
          exit 1

      - name: Cleanup container
        if: always()
        run: docker rm -f har-test || true